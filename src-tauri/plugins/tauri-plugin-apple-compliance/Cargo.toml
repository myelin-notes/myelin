[package]
name = "tauri-plugin-apple-compliance"
version = "0.1.0"
edition = "2021"
links = "tauri-plugin-apple-compliance"

[build-dependencies]
tauri-plugin = { version = "2", features = ["build"] }

[dependencies]
tauri = { version = "2" }
