[package]
name = "tauri-plugin-ocr"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = "2"
serde = { version = "1", features = ["derive"] }

[build-dependencies]
swift-rs = { version = "1", features = ["build"] }
