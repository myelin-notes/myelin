import { RepositorySection } from '../repository-section';

export function SyncSection() {
  return (
    <section id="sync" className="scroll-mt-12">
      <RepositorySection />
    </section>
  );
}
